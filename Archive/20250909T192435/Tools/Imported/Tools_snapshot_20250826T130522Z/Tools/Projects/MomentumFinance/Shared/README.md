Shared package: minimal module extracting Utilities for reuse.

- Product: SharedCore
- Targets: SharedCore (sources in `Utilities/`)

Usage: Add this Local Swift Package to your Xcode project or use `swift build` to validate.
