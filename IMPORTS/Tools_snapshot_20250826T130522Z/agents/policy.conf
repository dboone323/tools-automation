# Agent Policy Configuration
# Define allowed and blocked actions for agents and plugins
# Example:
# [agents]
# allow=build,debug,codegen
# block=delete,shutdown
# [plugins]
# allow=sample_hello,deploy
# block=experimental

[agents]
allow=build,debug,codegen
block=delete,shutdown

[plugins]
allow=sample_hello
block=experimental
